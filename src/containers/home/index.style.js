import styled from "styled-components";
import { mediaScreen, spacing } from "theme";
import { TabCommon } from "components";
import { metric } from "theme";

export const TabHome = styled(TabCommon)`
  background: white;
  border-radius: ${metric.radius};
`;
